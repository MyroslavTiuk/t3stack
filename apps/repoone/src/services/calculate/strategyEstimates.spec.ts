describe("services/calculate/strategyEstimates", () => {
  test("", () => {
    const expected = {};

    const result /*: StrategyEstimate*/ = {}; //strategyEstimates({}, {});

    expect(result).toEqual(expected);
  });
});
