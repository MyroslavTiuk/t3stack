export const MS_PER_DAY = 60 * 60 * 24 * 1000;

// For date-fns-tz:
//  Note: if switching back to date-fns-tz, some code may assume certain length during substr methods
// export const FMT_YYYY_MM_DD = 'yyyy-MM-dd';

// For moment-timezone:
export const FMT_YYYY_MM_DD = 'YYYYMMDD';
export const FMT_YYYY_MM_DD_HHMM = 'YYYYMMDD HH:mm:00';
