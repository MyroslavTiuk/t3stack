import React, { type FC, type ReactElement } from "react";

import { type UserProfileProps } from "./UserProfile.props";

const UserProfileView: FC<UserProfileProps> = (
  _props: UserProfileProps
): ReactElement<"div"> => {
  return <div />;
};

export default UserProfileView;
