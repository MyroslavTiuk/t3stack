{
  "name": "opcalc-database",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "postinstall": "prisma generate",
  "dependencies": {
    "@prisma/client": "^4.14.0"
  },
  "devDependencies": {
    "prisma": "^4.14.0"
  },
  "main": "./index.ts",
  "types": "./index.ts"
}
